pages:
  script:
  - mkdir .public
  - cp -r * .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master

algolia-jobs:on-schedule:
  image: node:latest
  only:
    - schedules
  script:
    - npm run start
